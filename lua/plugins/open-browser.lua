return {
  {
    "tyru/open-browser.vim",
    cmd = "OpenBrowser",
    Lazy = false, -- Lazy loads when OpenBrowser command is used
    -- You can also use other keys like 'keys', 'fn', or 'event' for different lazy loading triggers
  },
  -- ... other plugins
}
